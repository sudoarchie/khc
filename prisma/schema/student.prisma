model Student {
  id         String   @id @default(auto()) @map("_id") @db.ObjectId
  email      String   @unique
  mobileNo   String   @unique
  country    String
  name       String
  password   String
  payment    Boolean
  status     Boolean
  videoAllow Boolean
  createdAt  DateTime @default(now())
}
